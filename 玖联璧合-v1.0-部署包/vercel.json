{
  "buildCommand": "cd source && npm run build",
  "outputDirectory": "source/dist",
  "installCommand": "cd source && npm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
