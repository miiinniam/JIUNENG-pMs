/**
 * features/user/customer — 内部员工端客户管理功能入口
 * ─────────────────────────────────────────────────────────────────────────────
 * 导出客户 CRM 相关的操作函数（仅供内部员工使用，代理端不可访问）。
 * ─────────────────────────────────────────────────────────────────────────────
 */

// 客户数据操作通过 supabase 客户端直接访问 customers 表
// 如需封装，在此处添加具体函数

export { supabase } from '../../../lib/supabase';
export type { CustomerRow } from '../../../lib/types';
